services:
  server:
    build:
      context: .
    env_file:
      - .env
    environment:
      FLASK_ENV: production
      CORS_ORIGIN: http://localhost:4200
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    restart: unless-stopped
